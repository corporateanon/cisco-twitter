<CiscoIPPhoneMenu>
  <Title>Latest Tweets</Title>
  <Prompt></Prompt>

  {% for item in statuses %}
  <MenuItem>
    <Name>{{ item.getText()[0:60] }}</Name>
    <URL>{{ base }}/services/tweet/{{ item.status.id_str }}</URL>
  </MenuItem>
  {% endfor %}

</CiscoIPPhoneMenu>
