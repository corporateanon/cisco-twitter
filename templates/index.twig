<CiscoIPPhoneMenu>
  {% for service in services %}
  <MenuItem>
    <Name>{{ service.name }}</Name>
    <URL>{{ base_url() }}{{ path_for(service.route) }}</URL>
  </MenuItem>
  {% endfor %}

  <Title>My Services</Title>
</CiscoIPPhoneMenu>
