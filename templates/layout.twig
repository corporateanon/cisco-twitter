<!DOCTYPE html>
<html>
        <head>
            {% block head %}
                <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=0, minimum-scale=1.0, maximum-scale=1.0">
                <link rel="stylesheet" href="{{ base_url() }}/static/css/bootstrap/css/bootstrap.css">
                <link rel="stylesheet" href="{{ base_url() }}/static/css/bootstrap/css/bootstrap-theme.css">
                <link rel="stylesheet" href="{{ base_url() }}/static/css/font-awesome/css/font-awesome.css">
                <script src="{{ base_url() }}/static/js/jquery-2.2.3.min.js"></script>
                <script src="{{ base_url() }}/static/css/bootstrap/js/bootstrap.min.js"></script>
            {% endblock %}
        </head>
        <body>
            <div class="container">
                {% block content %}
                {% endblock %}
            </div>
        </body>
</html>
